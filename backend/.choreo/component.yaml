schemaVersion: "1.2"

endpoints:
  - name: rest_api       # tout en minuscules et underscore
    type: REST
    networkVisibility: Public
    context: /
    service:
      port: 8000         # obligatoire pour la validation
    expose:
      - path: /register
        public: true